/*
 * BabyPage Messages
 *
 * This contains all the text for the BabyPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BabyPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BabyPage container!',
  },
});
